create table tools ( id INTEGER PRIMARY KEY, kolor char(20), firma char(20), przedmiot char(20), stan INTEGER, login varchar(20));

create table users (id INTEGER PRIMARY KEY, login varchar(20), haslo varchar(20), imie varchar(20), nazwisko varchar(20)); 
