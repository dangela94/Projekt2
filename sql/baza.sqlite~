create table library ( id INTEGER PRIMARY KEY, imie char(20), nazwisko char(20), nazwa char(20), stan INTEGER);
insert into library (NULL, 'Henryk', 'Sienkiewicz', 'Krzyzacy', 0);

create table users (id INTEGER PRIMARY KEY, login varchar(20), haslo varchar(20), imie varchar(20), nazwisko varchar(20)); 
